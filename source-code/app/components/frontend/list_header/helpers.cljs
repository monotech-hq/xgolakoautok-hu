
(ns app.components.frontend.list-header.helpers)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

(defn header-attributes
  ; @param (keyword) header-id
  ; @param (map) header-props
  ;  {}
  ;
  ; @return (map)
  ;  {}
  [header-id header-props]
  {:style {:display :table-header}})
