
(ns app.home.frontend.handler.config)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @constant (maps in vector)
;  [{:name (metamorphic-content)}]
(def GROUP-ORDER [{:name :business}
                  {:name :crypto-currencies}
                  {:name :products-and-services}
                  {:name :vehicles}
                  {:name :content}
                  {:name :website}
                  {:name :settings}
                  {:name :other}])
