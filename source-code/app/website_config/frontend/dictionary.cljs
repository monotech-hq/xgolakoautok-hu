
(ns app.website-config.frontend.dictionary)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @constant (map)
(def BOOK {:website-config
           {:en "Website config"
            :hu "Webhely beállításai"}})
           
