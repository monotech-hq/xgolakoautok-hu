
(ns project.router.backend.main-routes)
   ;(:require [sente.api :as sente])

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @constant (map)
(def ROUTES {})
             ;:websocket/channel {:route-template "/chsk"
             ;                    :get  #(sente/ring-ajax-get-or-ws-handshake %)
             ;                    :post #(sente/ring-ajax-post                %)})
